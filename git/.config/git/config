[core]
	pager = delta

[pager]
	diff = diffnav
	show = diffnav
	log = diffnav

[delta]
	features = split-view
	navigate = true
	line-numbers = true
	syntax-theme = Catppuccin Mocha

[delta "split-view"]
	side-by-side = true

[delta "non-split-view"]
	side-by-side = false
